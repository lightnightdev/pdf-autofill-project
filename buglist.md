- When selecting a marker, the setting input fields don't match state
- Enable downloading the CSV template
- Add highlighting to rasterized LocEls
- When editing monospace-spacing, it doesn't update (keys)
    > Do this first
- Rasterize renderDoc for customText with spacing

- Only suppress keys if a LocEl is selected - unselect when opening Modal
- Unable to click-to-select a column without it doubling
- Save doubled columns separately... maybe an "Origin CSV Column" so it re-generates whenever a CSV is uploaded
- If only one page, close PDF Navigation, open Marker Data